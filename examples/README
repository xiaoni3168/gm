append([img])                       拼接图片
crop(x, y)                          裁剪图片 x:宽度 y:高度
extent(x, y)                        裁剪图片或resize之后的图片面积大小  x:宽度 y:高度
bitdepth(n)                         图片色深  n越小,图片越黑
blur(radius, [sigma])               图片模糊化 radius:模糊半径 sigma:模糊倍数 (模糊度=radius x sigma)
charcoal(n)                         炭描  n越大炭描越详细
chop(w, h, x, y)                    裁剪图片,保留裁剪之后的图片  w裁剪矩形宽度,h裁剪矩形高度
colorize(r, g, b)                   图片变色  r g b (0 ~ 255)
contrast(n)                         对比度 n越大,对比度越大
despeckle()                         图片除燥
monochrome()                        图片黑白化
emboss(n)                           图片浮雕
flip()                              图片翻转 上下
flop()                              图片翻转 左右
implode(n)                          图片中心内聚 0 < n < 1
lower(x, y)                         图片内陷
raise(x, y)                         图片突出
magnify()                           图片放大
minify()                            图片缩小
modulate()                          改变图像的亮度、饱和度和色调
negative()                          图片负片
paint(n)                            图片油画效果
